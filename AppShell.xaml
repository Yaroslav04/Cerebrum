<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Cerebrum.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Cerebrum"
    xmlns:view="clr-namespace:Cerebrum.Core.View"
    Shell.FlyoutBehavior="Flyout">

    <FlyoutItem Title="Головна сторінка">
        <ShellContent Route="MainPage" ContentTemplate="{DataTemplate view:MainPage}" />
    </FlyoutItem>
    
    <FlyoutItem Title="Тест">
        <ShellContent Route="MainPage" ContentTemplate="{DataTemplate view:MainPage}" />
    </FlyoutItem>

</Shell>
